{
  "name": "expopushserver",
  "version": "1.0.0",
  "description": "Sends push notifications to expo registered devices",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "expo-server-sdk": "^3.3.0"
  }
}
